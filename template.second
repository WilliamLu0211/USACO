
#define STR_SIZE 64

int main() {

  char fileIn[STR_SIZE] = PROMPT;
  strcat(fileIn, ".in");
  char fileOut[STR_SIZE] = PROMPT;
  strcat(fileOut, ".out");

  ifstream fin(fileIn);
  ofstream fout(fileOut);

  int n;
  fin >> n;

  return 0;
}
